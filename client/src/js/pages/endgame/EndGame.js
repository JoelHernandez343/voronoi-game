import Page from '../Page.js';

const EndGame = ({ states, game }) => {
  return (
    <Page>
      <div className="h-full max-h-full grid game-main">
        <h2>Este es el final</h2>
      </div>
    </Page>
  );
};

export default EndGame;
